# APROINT Homebrew Tap

Homebrew formulas for APROINT tools.

## Installation

```sh
brew install aproint/tap/copilot-cli
```

The `copilot-cli` formula installs the `copilot` binary from GitHub Release assets published by `aproint/copilot-cli`.
